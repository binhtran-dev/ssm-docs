Act as a Senior Software Engineer and Domain Expert in Java/Spring Boot.

I have an implementation plan/design that I need you to execute. Please write the complete, production-ready code for this implementation.

**Strict Code Quantity Requirements:**
1. **Clean Code & SOLID:** Adhere strictly to SOLID principles. Ensure methods are short, do only one thing (SRP), and use descriptive, intention-revealing names. Avoid “God classes” and deep nesting (arrow anti-pattern).
2. **Modern Syntax:** Utilize modern Java features (e.g., records, streams, `Optional`, switch expressions) and Lombok annotations where appropriate to reduce boilerplate.
3. **Robustness:** Include proper exception handling. Do not swallow exceptions; throw meaningful business or standard exceptions.
4. **Observability:** Include appropriate logging (using SLF4J/Logback) at the `INFO` level for business milestones and `DEBUG`/`ERROR` for troubleshooting. Do not log sensitive data.
5. **Immutability:** Favor immutability. Use `final` variables, immutable collections (e.g., `List.of()`), and constructor injection (via Spring’s `@RequiredArgsConstructor`).
6. **Documentation:** Add concise JavaDoc comments ONLY to public interfaces and complex class-level logic. Do not add obvious inline comments (let the code document itself).

**Here is the implementation plan/design to execute:**
[INSERT_HERE]